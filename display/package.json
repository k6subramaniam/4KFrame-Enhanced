{
  "name": "@4kframe/display",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/*.test.js"
  },
  "dependencies": {
    "@4kframe/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.15",
    "@types/node": "^22.7.0"
  }
}
