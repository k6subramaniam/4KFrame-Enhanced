{
  "name": "@4kframe/admin",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "rm -rf dist-test && tsc -p tsconfig.test.json && node --test dist-test/cropTapGesture.test.js && rm -rf dist-test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@4kframe/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.15"
  }
}
